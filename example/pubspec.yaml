name: example
description: Example.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  cloud_firestore:
  freezed_annotation:
  jacov:
    path: ..
  json_annotation:
dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
  pedantic_mono:
